node_modules/
frontend/node_modules/
dist/
dist-electron/
*.db
*.db-journal
.env
ecc-kaynak/

# Calisma artefaktlari (kaynak degil)
Ekran görüntüsü*.png
TEDARIKCIDEN LISTELER/

# Hassas ticari dokumanlar (sozlesme/tarife) — repoya girmez
UPS GÖNDERİ HESAPLAMA/
~$*.xlsx

# Muhasebe/urun calisma verileri (fiyat, dara, urun listeleri) — ticari gizli, repoya girmez
bizimhesap-ugyulama-ikas-ürünler/
/ikas-urunler*.xlsx

# Urun eslestirme ciktilari (SKU/fiyat/barkod calisma dosyalari) — ticari gizli.
# REPO PUBLIC: buraya giren her sey internete acilir, gecmisten silmek de zordur.
URUN-ESLESTIRME/

# Yeni urun giris calismasi: her urun icin bir alt klasor (gorseller + ozellik dosyalari).
# Ozellik dosyalari alis fiyati/maliyet icerebilir ve REPO PUBLIC.
ÜRÜN GİRİŞİ/

# Tedarikci kataloglari (fiyat listesi icerir)
KATALOG*.pdf
*KATALOG*.pdf

# Sirket verileri (fatura, sozlesme, komisyon, fiyat listeleri) — REPO PUBLIC, ASLA girmez
FATURALAR/
HEPSIBURADA/
TRENDYOL/
MAL KABUL/

# Taranmis belge gorselleri (irsaliye vb. kaynak belgeler) — kok dizindeki jpeg/jpg
/*.jpeg
/*.jpg

# Yerel calisma/gecici dosyalar.
# KOKTE '_' ile baslayan hicbir sey uygulama kodu degildir (11.09 dogrulandi:
# takipli tek bir '_' dosyasi yok). Betik + gunluk + ara cikti hepsi buraya girer.
/_*
*.output

# GENEL KURAL: Uygulama kodu/dokumani DISINDA hicbir sey (sirket verisi,
# fatura, fiyat, musteri bilgisi, gorsel calisma dosyasi) bu repoya eklenmez.

# UPS API dokumanlari (vendor PDF/XLS) — repoya girmez, yerel kalir
UPS KARGO ENTEGRASYONU/

# Reklam kampanyasi calisma dosyalari (butce/hedefleme/token notlari) — ticari, repoya girmez
REKLAM-KAMPANYALARI/

# Yayin gunlugu (yerel)
publish.log

# Arama indeksi (urun adi/fiyat = ticari veri) — repoya girmez, deploy sirasinda uretilir
cloudflare/arama-worker/veri/

# Calisma verisi (tedarikci PDF, ham video) — REPO PUBLIC, uygulama kodu degil
Dara/
video edit/

# Claude Code calisma kilidi (yerel)
.claude/scheduled_tasks.lock
